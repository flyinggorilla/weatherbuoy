#ifndef MAIN_SIMULATEMAXIMET_H_
#define MAIN_SIMULATEMAXIMET_H_

/*
#include "esp_event.h"
#include "EspString.h"
#include "Serial.h"

enum MaximetModel {
    none,
    gmx501,
    gmx200gps
};

class SimulateMaximet {
public:
	SimulateMaximet();
	virtual ~SimulateMaximet();

    // start the task
    void Start(int gpioRX, int gpioTX, MaximetModel model);
    void Send(float temperature, double longitude, double latitude );


private:
    void SendLine(const char* text);
    void SendLine(String &line);

    Serial *mpSerial;
    int mgpioRX;
    int mgpioTX;
    MaximetModel mModel;
    
}; */

#endif 


